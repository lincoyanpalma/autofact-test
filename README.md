

## Autofact Instrucciones

- Copiar <b>.env.example</b> a <b>.env</b>
- Modificar los datos de la BD según accesos del entorno (DB_DATABASE,DB_USER y DB_PASSWORD)
- Ejecutar <b>composer install</b>
- Ejecutrar <b>php artisan migrate --seed</b> para correr migraciones y seeders
- Ejecutar npm install && npm run dev para compilar templates
